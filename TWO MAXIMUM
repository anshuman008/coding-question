// find two maximum number
#include <iostream>
#include<vector>
using namespace std;
int main() {

vector<int>v;
int size;
cin>>size;

int value;
for(int i =0;i<size;i++){
    cin>>value;
    v.push_back(value);
}

int max1;
int max2;
for(int i = 0;i<v.size();i++){
    if(v[i]>=max1){
        max2 = max1;
        max1 = v[i];
    }
  
}
cout<<max1<<" "<<max2;
    return 0;
}
